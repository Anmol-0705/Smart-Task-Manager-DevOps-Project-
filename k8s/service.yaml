# apiVersion: v1
# kind: Service
# metadata:
#   name: task-service
# spec:
#   type: NodePort
#   selector:
#     app: task-manager
#   ports:
#     - port: 5000
#       targetPort: 5000
#       nodePort: 30007


apiVersion: v1
kind: Service
metadata:
  name: task-service
spec:
  type: NodePort
  selector:
    app: task-app
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007